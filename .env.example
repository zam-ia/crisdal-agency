# Pixel 3971063776548749 is public and is configured in app/lib/config.ts.
# Optional server-side Conversions API. Never prefix this token with NEXT_PUBLIC_.
META_CAPI_ACCESS_TOKEN=
# Set the currently supported Graph API version when enabling CAPI (e.g. vXX.0).
META_GRAPH_API_VERSION=
# Optional Meta Test Events code. Remove for live campaign delivery.
META_TEST_EVENT_CODE=
